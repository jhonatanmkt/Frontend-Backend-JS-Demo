# Frontend-Backend-JS-Demo
O JavaScript opera nativamente no navegador (Front-end) e como lógica pura (Backend/Console). Esta simulação do processo legislativo compara a implementação da mesma estrutura de dados via manipulação do DOM (JS + HTML + CSS) versus lógica independente (JavaScript Puro), evidenciando a separação da lógica central da sua camada de apresentação.
